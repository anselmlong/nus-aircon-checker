# Telegram
TELEGRAM_BOT_TOKEN=

# Optional hard allowlist; comma-separated Telegram user IDs.
# If empty, any user can query the bot.
TELEGRAM_ALLOWED_USER_IDS=

# Encryption key for persisting credentials across restarts.
# At least 16 characters. If not set, credentials are stored in-memory only.
# If not set, the bot generates/stores one in .evs-storage.key on first start.
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# Users log in with their own credentials via /login.
